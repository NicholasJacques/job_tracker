<h1>All Categories</h1>

<ul>
<% @categories.each do |category| %>
  <li><%= link_to category.title, category_path(category) %> <%= link_to "Edit", edit_category_path(category) %></li>
<% end %>
</ul>